services:
  - type: web
    name: michela-backend
    env: python
    runtime: python-3.12
    buildCommand: pip install -r requirements.txt
    startCommand: python src/app/logic/api.py
    envVars:
      - key: GOOGLE_CREDENTIALS
        value: '{"type":"service_account", ...}'  # keys/michela-*.jsonの内容を貼り付け